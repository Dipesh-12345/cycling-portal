# Copy this file to .env and edit values before running docker-compose
DB_HOST=db
DB_NAME=cycling
DB_USER=root
DB_PASS=password

# MySQL root password (used by docker-compose to init DB)
MYSQL_ROOT_PASSWORD=password
